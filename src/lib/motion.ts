/** Smooth ease-out curve used across route and section animations. */
export const easeSmooth = [0.22, 1, 0.36, 1] as const

export const duration = {
  page: 0.38,
  section: 0.45,
  fast: 0.22,
} as const
